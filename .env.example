# Jira Server personal access token
JIRA_TOKEN=your-personal-access-token

# PostgreSQL connection (used when output.target = "postgres")
POSTGRES_DSN=postgresql://user:password@localhost:5432/jira_data

# Optional overrides (these can also live in config.toml)
# JIRA_BASE_URL=https://jira.example.com
